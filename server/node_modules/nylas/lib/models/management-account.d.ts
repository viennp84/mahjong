import ManagementModel from './management-model';
export default class ManagementAccount extends ManagementModel {
    billingState?: string;
    emailAddress?: string;
    namespaceId?: string;
    provider?: string;
    syncState?: string;
    trial?: boolean;
    upgrade(): Promise<any>;
    downgrade(): Promise<any>;
    revokeAll(keep_access_token?: string): Promise<any>;
    ipAddresses(): Promise<any>;
    tokenInfo(access_token?: string): Promise<any>;
}
